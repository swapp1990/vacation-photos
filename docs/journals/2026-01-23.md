# January 23, 2026

## App Clip UI Fixes

- Text "Swap shared a vacation" was cut off - fixed with `frame(maxWidth: .infinity)` + `minimumScaleFactor(0.5)`
- Added swipeable TabView for photo preview with page dots
- Close button wasn't visible - moved to ZStack overlay on top
- Made UI lazy load: show vacation info immediately, thumbnails load in background

## Save Photos Auto-Recovery

- "Photo file not found" error when saving shared photos (iOS clears CloudKit cache)
- Fixed: auto re-fetch from CloudKit if file missing, then save

## Environment Fixes

- EAS build failing due to Node 16 - switched default to Node 20, removed 16
- Learned: Safari links load production App Clip, not TestFlight - use Local Experiences to test

## Build 18 submitted to TestFlight, version 1.1.3 created for App Store review
