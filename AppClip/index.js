/**
 * App Clip Entry Point
 * This is a lightweight version of the app that loads instantly
 * when users tap a Universal Link without having the app installed.
 */
import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('VacationPhotosClip', () => App);
